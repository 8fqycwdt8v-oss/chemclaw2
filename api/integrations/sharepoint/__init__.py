"""SharePoint / OneDrive integration via Microsoft Graph.

See `docs/plans/sharepoint-drive-sync.md` for the full plan. This package
holds the Graph connector; the sync worker that drives it is added in a
later slice.
"""
